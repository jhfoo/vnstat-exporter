echo === Assumptions ===
echo 1. Installed via bin/install.
echo 2. Supported shell: zsh
echo 3. Already installed and configured: vnstat
echo ===================

venv/bin/uvicorn src.main:app --reload --host 0.0.0.0 --port 8080